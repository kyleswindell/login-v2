# Ephemeral Memory

Use this directory for short-lived session compression and temporary working memory.

Rules:

- keep contents brief
- delete or rewrite aggressively
- do not let this become a second knowledge base
- do not store secrets or sensitive values
- promote durable truth into canonical docs, stable memory, or the correct instruction owner when needed
