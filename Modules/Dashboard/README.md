# Dashboard Module

`Modules/Dashboard` is the first real module package proof. It owns the authenticated `/dashboard` route and a blank module-owned main view.

Dashboard widgets, saved widget layouts, and customization behavior are intentionally deferred for a from-scratch rebuild.
