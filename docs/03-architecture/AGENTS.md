# docs/03-architecture AGENTS.md

## Purpose

System structure only. This branch owns architectural boundaries, subsystem structure, tenancy shape, stack structure, and platform layering.

## Read Order

1. Read `index.md`.
2. Open the narrow subsystem or architecture file that owns the affected structure.
3. Cross-check decisions in `docs/01-decisions/` only when the architecture doc points to a decision or the change would alter a locked direction.

## Avoid

- Do not place feature behavior, workflow steps, schema details, or implementation sequencing here.
- Do not read every subsystem file for a local code change.
